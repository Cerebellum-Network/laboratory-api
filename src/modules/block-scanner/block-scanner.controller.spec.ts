// import {Test, TestingModule} from '@nestjs/testing';
// import {BlockScannerController} from './block-scanner.controller';
// import {BlockScannerService} from './block-scanner.service';

describe('BlockScannerController', () => {
  // let blockScannerController: BlockScannerController;

  beforeEach(async () => {
    // const app: TestingModule = await Test.createTestingModule({
    //   controllers: [BlockScannerController],
    //   providers: [BlockScannerService],
    // }).compile();

    // blockScannerController = app.get<BlockScannerController>(BlockScannerController);
  });

  describe('root', () => {
    it('should return "Hello World!"', () => {
      return null;
    });
  });
});
